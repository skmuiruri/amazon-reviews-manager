
Assumptions

1. The input file contains newline delimited JSON file, each line containing one JSON object. If any of the lines
    has invalid JSON then the streaming process will fail with the assumption that all other incoming values will
    also be invalid.